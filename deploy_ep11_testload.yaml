# a simple cex ep11 fake testload
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cex-ep11-testload
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cex-ep11-testload
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cex-ep11-testload
    spec:
      containers:
      - image: 'busybox'
        name: cex-ep11-testload
        command: ["/bin/sh", "-c", "while true; do echo test-crypto-load; sleep 30; done"]
        resources:
          limits:
            ibm.com/cex-ep11: 1